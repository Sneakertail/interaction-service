apiVersion: v1
kind: Service
metadata:
  name: interaction-service-preview
  namespace: dev
spec:
  selector:
    app: interaction-service
  ports:
    - name: http
      port: 3004
      targetPort: 3004

